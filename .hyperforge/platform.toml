# www.hypermemetic.ai → 301 to apex hypermemetic.ai
#
# No build step: the repo IS the served content (public/_redirects + a
# fallback index.html). install_command and command both empty per
# schema v1.

version = "1"

[site]
kind = "static"
domain = "www.hypermemetic.ai"
zone_id = "a81fdc3125a732050ea621b5c7ccc531"

[build]
command = ""
output_dir = "public"
install_command = ""

[deploy]
project_name = "hypermemetic-www"
production_branch = "main"
